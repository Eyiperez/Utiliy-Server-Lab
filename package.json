{
  "name": "utility-server-lab",
  "version": "1.0.0",
  "description": "An Express server that has a math add, math multiply and gif route, each take query parameters through GET requests.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Erika Perez Valle",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "giphy-js-sdk-core": "^1.0.6"
  }
}
